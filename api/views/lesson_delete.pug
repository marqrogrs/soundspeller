extends layout

block content
    h1 Delete Lesson with id: #{lesson.lesson_id}

    p Do you really want to delete this Author?

    form(method='POST' action='')
        div.form-group
            input#id.form-control(type='hidden',name='id', required='true', value=lesson._id )
        button.btn.btn-primary(type='submit') Delete
